# Couchbase Query Analyzer - Python Server Dependencies
# Install with: pip install -r requirements.txt

# Core web framework
flask>=3.0.0
flask-cors>=4.0.0
# Production WSGI server (used in Docker; local dev still uses flask run / app.run)
gunicorn>=21.2.0

# Couchbase SDK
couchbase>=4.1.0

# HTTP and AI clients
requests>=2.31.0
urllib3>=2.0.0
openai>=1.0.0

# Utilities
icecream>=2.1.3
# toon-python is NOT on PyPI; install separately from GitLab:
#   pip install 'toon-python @ git+https://gitlab.com/KanTakahiro/toon-python.git'
# (The Dockerfile installs it for you. app.py also has a runtime fallback.)
python-dotenv>=1.0.0
platformdirs>=4.0.0

# Platform-specific
rumps>=0.4.0; sys_platform == 'darwin'
pystray>=0.19.0; sys_platform == 'win32'
Pillow>=10.0.0; sys_platform == 'win32'