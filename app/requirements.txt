flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.0.0
couchbase>=4.0.0
icecream>=2.1.3
requests>=2.31.0
urllib3>=2.0.0
platformdirs>=4.0.0
cffi>=1.16.0
# TOON encoder. The original `toon_python` lives in a private GitLab repo and
# is not publicly installable. We use the public `python-toon` package (module
# name `toon`); a `toon_python.py` shim at the project root re-exports its API
# under the `toon_python` name so existing `import toon_python` call sites
# (e.g. in app_base.py) keep working unchanged.
python-toon>=0.1.0
# Couchbase Lite Python bindings are NOT on PyPI; the Dockerfile and
# PyInstaller specs build them from couchbaselabs/couchbase-lite-python
# against libcblite (libcblite.so / libcblite.dylib / cblite.dll).
